"use strict";

/*
Необходимо попросить пользователя ввести число.
Если пользователь ввел отличное от числа значение, необходимо вывести в консоль
строку: "Значение задано неверно", иначе необходимо будет вызвать функцию 
(нужно будет ее создать), которая будет принимать введенное пользователем 
число. Функция должна вычесть из переданного ей числа 13% и вывести в консоль 
сообщение "Размер заработной платы за вычетом налогов равен N."
*/

// Функция для вычисления заработной платы за вычетом налогов

function calculateSalaryAfterTaxes(salary) {
    // Вычитаем 13% от заработной платы

    const salaryAfterTaxes = salary * 0.87;
  
    // Выводим результат в консоль

    console.log(`Размер заработной платы за вычетом налогов равен ${salaryAfterTaxes}.`);
  }
  
  // Запрашиваем ввод числа у пользователя

  const input = prompt("Введите число:");
  
  // Проверяем, является ли введенное значение числом

  if (isNaN(input)) {

    // Если введено не число, выводим сообщение об ошибке

    console.log("Значение задано неверно");
  } else {

    // Если введено число, преобразуем его в число и вызываем функцию расчета заработной платы

    const salary = Number(input);
    calculateSalaryAfterTaxes(salary);
  }
